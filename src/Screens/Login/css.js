import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: 'center'
    },
    logo: {
        marginVertical: 10,
    },
    form: {
        marginBottom: 10
    },
    button: {
        marginBottom: 20
    },
    footer: {
        marginBottom: 50
    }
});

export { styles };
