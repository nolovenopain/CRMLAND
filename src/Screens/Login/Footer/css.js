import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        justifyContent: "center",
        alignItems: "center",
    },
    register: {
        fontSize: 13,
        marginBottom: 10
    },
    forgotPassword: {
        fontSize: 13
    },
    blue: {
        color: "#5abebd"
    }
});

export { styles };
