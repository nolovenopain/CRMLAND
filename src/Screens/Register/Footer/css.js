import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        marginTop: 20
    },
    backToLogin: {
        fontSize: 13
    },
    blue: {
        color: "#5abebd"
    }
});

export { styles };
