import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
    },
    header: {
        flexDirection: "row"
    },
    logo: {},
    title: {
        fontSize: 23,
        fontWeight: "bold",
        color: "#5abebd",
        marginLeft: 15
    }
});

export { styles };
